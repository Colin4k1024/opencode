# OpenCode server URL when running the app in browser (dev or production without desktop).
# Default is http://localhost:4096. Start the server with: opencode serve --port 4096
# VITE_OPENCODE_SERVER_HOST=localhost
# VITE_OPENCODE_SERVER_PORT=4096
