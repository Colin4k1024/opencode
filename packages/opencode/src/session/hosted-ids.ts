/** Session IDs for hosted (background) runs. When a session is in this set, permission/question prompts are auto-resolved (allow / first option). */
export const hostedSessionIDs = new Set<string>()
